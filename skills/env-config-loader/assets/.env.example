# Copy to .env and fill in real values. Keep the real .env gitignored.
# Placeholders only - never commit real secrets.

API_BASE=https://api.example.com
RUN_LIMIT=50
DRY_RUN=false
TAGS=alpha,beta,gamma

# Secrets are read from the environment too; do not put real ones here.
# API_KEY=your-api-key-here
