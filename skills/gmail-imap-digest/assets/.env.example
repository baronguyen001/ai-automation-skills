# Copy to .env and fill in. Use a provider app-password with IMAP enabled,
# NOT your normal login password. Keep .env out of version control.
IMAP_HOST=imap.example.com
IMAP_USER=you@example.com
IMAP_APP_PASSWORD=your-16-char-app-password
